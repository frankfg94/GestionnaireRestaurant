#pragma once

enum PlacementType
{
	espace,				// les tables sont positionnés de la façon la plus spatieuse possible
	compresse,			// les tables sont rapprochées le plus possible
	fete,				// les tables sont alignées comme dans un banquet
	compresseCouloir	// les tables sont rapprochées mais avec un passage toutes les deux rangées
}; 
